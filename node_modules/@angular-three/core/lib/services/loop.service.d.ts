import { NgZone } from '@angular/core';
import { NgtAnimationFrameStore } from '../stores/animation-frame.store';
import { NgtStore } from '../stores/store';
import { NgtAnimationFrameStoreState, NgtState } from '../types';
import * as i0 from "@angular/core";
export declare class NgtLoopService {
    #private;
    private store;
    private animationFrameStore;
    private ngZone;
    constructor(store: NgtStore, animationFrameStore: NgtAnimationFrameStore, ngZone: NgZone);
    render(timestamp: number, state: NgtState, animationFrameState: NgtAnimationFrameStoreState): number;
    loop(timestamp: number): number | undefined;
    invalidate(state?: NgtState): void;
    static ɵfac: i0.ɵɵFactoryDeclaration<NgtLoopService, never>;
    static ɵprov: i0.ɵɵInjectableDeclaration<NgtLoopService>;
}
